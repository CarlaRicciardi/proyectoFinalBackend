const express = require("express");
const routerCarts = express.Router();
const controller = require("../controller/carts.js");

// routerProducts.get("/cart", controller.);

// module.exports = routerCarts;
